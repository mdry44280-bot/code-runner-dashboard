# Code Runner - منصة تشغيل الأكواد البرمجية 🚀

منصة مجانية وسهلة الاستخدام لتشغيل الأكواد البرمجية (Python) بشكل دائم على السحابة دون انقطاع.

## المميزات ✨

- 🌐 **تشغيل دائم 24/7**: لا توقف، لا انقطاع
- 📤 **رفع الأكواد بسهولة**: واجهة API بسيطة لرفع ملفات Python
- 🎮 **لوحة تحكم متقدمة**: مراقبة الأكواد الجارية والسجلات
- 📊 **معلومات الأداء**: CPU، الذاكرة، وقت التشغيل
- 🔄 **إعادة تشغيل تلقائية**: الخادم يعيد تشغيل الأكواد تلقائياً عند التعطل
- 🆓 **مجاني تماماً**: لا توجد رسوم أو تكاليف مخفية

## طريقة الاستخدام 📖

### 1. رفع الكود
```bash
curl -X POST -F "file=@your_script.py" https://your-domain/upload
```

### 2. تشغيل الكود
```bash
curl -X POST https://your-domain/start/your_script.py
```

### 3. مراقبة الحالة
```bash
curl https://your-domain/status/your_script.py
```

### 4. الحصول على السجلات
```bash
curl https://your-domain/logs/your_script.py
```

### 5. إيقاف الكود
```bash
curl -X POST https://your-domain/stop/your_script.py
```

## API Endpoints 🔌

| الطريقة | المسار | الوصف |
|--------|--------|--------|
| GET | `/` | معلومات عامة |
| GET | `/health` | فحص صحة الخادم |
| POST | `/upload` | رفع ملف كود |
| POST | `/start/{script_name}` | تشغيل السكريبت |
| POST | `/stop/{script_name}` | إيقاف السكريبت |
| GET | `/scripts` | قائمة الملفات |
| GET | `/status/{script_name}` | حالة السكريبت |
| GET | `/logs/{script_name}` | سجلات السكريبت |
| GET | `/dashboard` | لوحة التحكم |
| POST | `/restart-all` | إعادة تشغيل الكل |

## الاستضافة على Koyeb 🚀

### الخطوات:
1. انسخ هذا المستودع إلى GitHub
2. اذهب إلى [Koyeb.com](https://www.koyeb.com)
3. اختر "Deploy from GitHub"
4. اربط مستودعك
5. اختر "Python" كنوع التطبيق
6. انقر "Deploy"

## المتطلبات 📦

- Python 3.8+
- FastAPI
- Uvicorn
- psutil

## الترخيص 📄

مفتوح المصدر - استخدم بحرية

---

**تم التطوير بواسطة Manus AI** 🤖
